// Thin re-export so the test file can import the pure decision function
// with a short, obvious path, matching the article's code samples.
export { decide, orderExpectedQty, recordedReducedQty } from "./repair-double-stock.js";
